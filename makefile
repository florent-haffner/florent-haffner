all: init

    init:
	$(shell ./init.sh)
    upgrade:
	$(shell echo 'todo for macos + linux')

    install-db:
        $(shell echo 'todo for postgre)
    backup-db:
        $(shell echo 'todo for postgre)
